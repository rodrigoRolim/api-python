to execute this project on development mode issue this commands:
```
$ vistualenv venv
$ source venv/bin/activate
$ pip install flask 
$ flask-jsonpify   
$ flask-sqlalchemy flask-restful
$ pip freeze
